# MySQL Reactive Datasource Configuration
quarkus.datasource.db-kind=mysql
quarkus.datasource.username=root
quarkus.datasource.password=admin
quarkus.datasource.reactive.url=vertx-reactive:mysql://localhost:3306/patientjournaldb

# Hibernate ORM Configuration
quarkus.hibernate-orm.log.sql=true

# CORS Configuration (for React frontend)
#quarkus.http.cors=true
#quarkus.http.cors.origins=http://localhost:3000
#quarkus.http.cors.headers=accept,authorization,content-type,x-requested-with
#quarkus.http.cors.methods=GET,POST,PUT,DELETE,OPTIONS

# Keycloak/OIDC Configuration (add later when setting up authentication)
# quarkus.oidc.auth-server-url=http://localhost:8180/realms/patientjournal
# quarkus.oidc.client-id=backend-service
# quarkus.oidc.credentials.secret=your_client_secret
